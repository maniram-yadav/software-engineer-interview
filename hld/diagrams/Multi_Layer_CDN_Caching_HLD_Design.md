# Design a Multi-Layer Caching Architecture for a Global CDN — High-Level Design Document

## 1. Requirements

### Functional Requirements
- Serve static and semi-static content (images, video, JS/CSS, API responses) with minimal latency worldwide
- Support cache invalidation when origin content changes
- Handle both cacheable (static assets) and dynamic (personalized/API) content appropriately
- Support cache purging on-demand for urgent content updates

### Non-Functional Requirements
- **Global low latency:** Content should be served from a location physically close to each user, worldwide
- **Origin protection:** The origin server must be shielded from the full weight of global traffic — cache layers should absorb the overwhelming majority of requests
- **Cache coherence:** Different cache layers/nodes shouldn't serve wildly inconsistent versions of content for extended periods
- **High cache hit ratio:** The economic and performance value of a CDN depends on minimizing origin round-trips

### Back-of-Envelope Estimation
| Metric | Value |
|---|---|
| Global edge locations | 100-300+ points of presence (PoPs) |
| Total requests/sec (platform-wide) | Millions |
| Target cache hit ratio | 90-99%+ for static content |
| Origin traffic (after caching) | Should be a small fraction of total (ideally <5%) |

---

## 2. High-Level Architecture — The Three-Tier Model

```mermaid
flowchart TB
    subgraph Users["End Users (globally distributed)"]
        U1["User (Tokyo)"]
        U2["User (London)"]
        U3["User (São Paulo)"]
    end

    subgraph EdgeTier["Tier 1: Edge PoPs<br/>(100s of locations, closest to users)"]
        Edge1["Edge PoP - Tokyo"]
        Edge2["Edge PoP - London"]
        Edge3["Edge PoP - São Paulo"]
    end

    subgraph RegionalTier["Tier 2: Regional/Mid-Tier Caches<br/>(fewer, larger, aggregation points)"]
        Regional1["Regional Cache - APAC"]
        Regional2["Regional Cache - EMEA"]
        Regional3["Regional Cache - Americas"]
    end

    subgraph OriginTier["Tier 3: Origin Shield + Origin"]
        Shield["Origin Shield<br/>(single logical entry point)"]
        Origin["Origin Server<br/>(source of truth)"]
    end

    U1 --> Edge1
    U2 --> Edge2
    U3 --> Edge3

    Edge1 -->|"cache miss"| Regional1
    Edge2 -->|"cache miss"| Regional2
    Edge3 -->|"cache miss"| Regional3

    Regional1 -->|"cache miss"| Shield
    Regional2 -->|"cache miss"| Shield
    Regional3 -->|"cache miss"| Shield

    Shield -->|"cache miss<br/>(rare)"| Origin
```

**Key idea:** Each tier exists to absorb misses from the tier below it, progressively funneling traffic down toward the origin — by the time a request reaches the origin, it represents the tiny fraction of truly novel/uncached content across the ENTIRE global user base, not just one region. This tiered funneling is what makes CDNs viable at global scale — without it, popular content would generate redundant origin fetches from every single edge location independently.

---

## 3. Request Flow — Cache Hit at Edge (Common Case)

```mermaid
sequenceDiagram
    participant User as User (Tokyo)
    participant Edge as Edge PoP (Tokyo)

    User->>Edge: GET /images/product123.jpg
    Edge->>Edge: Check local cache

    alt Cache HIT (common — ~90%+ of requests)
        Edge-->>User: Serve directly from edge<br/>(fast, single-digit ms latency,<br/>no further network hops needed)
    end
```

---

## 4. Request Flow — Cache Miss Cascading Through Tiers

```mermaid
sequenceDiagram
    participant User as User (Tokyo)
    participant Edge as Edge PoP (Tokyo)
    participant Regional as Regional Cache (APAC)
    participant Shield as Origin Shield
    participant Origin as Origin Server

    User->>Edge: GET /images/new-product.jpg
    Edge->>Edge: Check local cache — MISS

    Edge->>Regional: Forward request
    Regional->>Regional: Check regional cache — MISS

    Regional->>Shield: Forward request
    Shield->>Shield: Check shield-level cache — MISS<br/>(this is the FIRST time<br/>ANY edge/regional location<br/>globally has requested this)

    Shield->>Origin: Fetch from origin<br/>(the ONLY origin request<br/>generated by this entire<br/>cascade, despite the miss<br/>occurring independently at<br/>edge and regional tiers too)
    Origin-->>Shield: Content + cache headers<br/>(Cache-Control, ETag, etc.)

    Shield->>Shield: Cache the response
    Shield-->>Regional: Return content
    Regional->>Regional: Cache the response
    Regional-->>Edge: Return content
    Edge->>Edge: Cache the response
    Edge-->>User: Serve content
```

**Why the Origin Shield is critical:** Without it, EVERY regional cache experiencing a simultaneous miss for the same popular new content would independently hit the origin — potentially hundreds of redundant origin requests for the exact same content. The shield acts as a single, global coalescing point that guarantees the origin sees at most one request for a given piece of content during any cache-miss window, regardless of how many edge/regional locations are simultaneously requesting it.

---

## 5. Request Coalescing at Each Tier (Avoiding Duplicate Fetches)

```mermaid
sequenceDiagram
    participant U1 as User Request 1
    participant U2 as User Request 2 (near-simultaneous)
    participant Edge as Edge PoP

    U1->>Edge: GET /viral-video.mp4 (cache miss)
    Edge->>Edge: No in-flight fetch for this<br/>key yet — initiate fetch,<br/>mark as IN-FLIGHT

    U2->>Edge: GET /viral-video.mp4<br/>(arrives while fetch still in-flight)
    Edge->>Edge: Detect: fetch already<br/>in-flight for this key —<br/>DON'T initiate a second<br/>fetch, just wait

    Note over Edge: Original fetch completes
    Edge-->>U1: Serve content
    Edge-->>U2: Serve content<br/>(same fetched content,<br/>no duplicate origin/upstream<br/>request was made)
```

*This is the same request-coalescing pattern used to prevent thundering herd/cache stampede in the Distributed Cache design — applied here at every tier of the CDN hierarchy, since a popular piece of content going viral can generate a massive burst of near-simultaneous requests at any single edge location.*

---

## 6. Cache Invalidation Strategies

```mermaid
flowchart TB
    A["Origin content changes —<br/>how do caches find out?"] --> B{"Invalidation Strategy"}

    B --> C["TTL-based Expiration<br/>(passive)"]
    C --> C1["Content cached with a<br/>Cache-Control max-age header —<br/>automatically considered<br/>stale after that duration,<br/>re-fetched on next request"]
    C --> C2["PRO: simple, no explicit<br/>action needed<br/>CON: content can be stale<br/>for up to the full TTL<br/>duration after a change"]

    B --> D["Explicit Purge<br/>(active)"]
    D --> D1["Origin/CMS explicitly tells<br/>the CDN: 'invalidate this<br/>specific URL/path NOW,'<br/>propagated to all cache tiers"]
    D --> D2["PRO: near-immediate<br/>invalidation for urgent<br/>updates (e.g., a legal<br/>takedown, a pricing error)<br/>CON: propagation to<br/>hundreds of edge locations<br/>globally takes some time<br/>and coordination"]

    E["Production systems use BOTH:<br/>TTL for routine content<br/>freshness, explicit purge<br/>as an escape hatch for<br/>urgent/exceptional cases"] -.-> D
```

---

## 7. Explicit Purge Propagation — Detailed Sequence

```mermaid
sequenceDiagram
    participant Origin as Origin/CMS
    participant PurgeAPI as CDN Purge API
    participant PurgeQueue as Purge Propagation Queue
    participant Shield as Origin Shield
    participant Regional as Regional Caches (many)
    participant Edge as Edge PoPs (many, globally)

    Origin->>PurgeAPI: Purge request:<br/>/images/product123.jpg
    PurgeAPI->>PurgeQueue: Publish purge event

    PurgeQueue->>Shield: Invalidate cached entry
    PurgeQueue->>Regional: Invalidate cached entry<br/>(fanned out to ALL regional<br/>caches, in parallel)
    PurgeQueue->>Edge: Invalidate cached entry<br/>(fanned out to ALL edge<br/>PoPs globally, in parallel)

    Note over Edge: Propagation to hundreds<br/>of globally distributed edge<br/>locations isn't truly<br/>instantaneous — typically<br/>completes within seconds,<br/>not milliseconds, due to<br/>the sheer geographic fan-out<br/>required
```

---

## 8. Cache Key Design & Content Variation

```mermaid
flowchart TB
    A["What determines a<br/>cache key for a given request?"] --> B["Base: the URL path<br/>(/images/product123.jpg)"]
    A --> C["Vary by: specific request<br/>headers/parameters that<br/>affect the RESPONSE content"]

    C --> D["Example: Accept-Encoding<br/>(gzip vs non-gzip response<br/>bodies must be cached<br/>SEPARATELY)"]
    C --> E["Example: device type<br/>(mobile vs desktop image<br/>variants)"]
    C --> F["Example: geographic region<br/>(if content is region-locked<br/>or localized)"]

    G["Getting cache key design<br/>WRONG in either direction<br/>causes real problems:"] --> H["Too NARROW a key<br/>(ignoring a variation<br/>dimension) → serves the<br/>WRONG content variant to<br/>some users (correctness bug)"]
    G --> I["Too BROAD a key<br/>(varying on irrelevant<br/>headers) → fragments the<br/>cache unnecessarily,<br/>tanking the hit ratio"]
```

---

## 9. Dynamic/Personalized Content Handling

```mermaid
flowchart TB
    A["Not all content is<br/>cacheable the same way"] --> B{"Content Type"}

    B --> C["Fully static<br/>(images, JS/CSS bundles)"]
    C --> C1["Cache aggressively,<br/>long TTL, same for<br/>all users"]

    B --> D["Semi-static, cacheable<br/>with short TTL<br/>(API responses that change<br/>infrequently, e.g., product<br/>catalog listing)"]
    D --> D1["Cache with short TTL<br/>(seconds to minutes),<br/>accepting minor staleness"]

    B --> E["Fully dynamic/personalized<br/>(user's shopping cart,<br/>account dashboard)"]
    E --> E1["NOT cached at the shared<br/>CDN tiers at all — CDN<br/>passes these requests<br/>straight through to origin,<br/>or uses edge COMPUTE<br/>(edge functions) for<br/>lightweight personalization<br/>logic without full caching"]
```

---

## 10. Component Responsibilities Summary

```mermaid
mindmap
  root((Multi-Layer CDN HLD))
    Edge PoPs
      Closest to end users
      Highest request volume, absorbs most traffic
    Regional Caches
      Aggregation point per region
      Reduces edge-to-shield fan-out
    Origin Shield
      Single global coalescing point
      Guarantees minimal origin load
    Origin Server
      Source of truth
      Sees only true cache misses
    Purge Propagation System
      Explicit invalidation fan-out
      Complements TTL-based expiry
    Request Coalescing
      Per-tier in-flight fetch tracking
      Prevents duplicate upstream fetches
```

---

## 11. Key Design Decisions & Tradeoffs

| Decision | Choice | Why |
|---|---|---|
| Architecture | Three-tier (edge, regional, origin shield) | Progressively funnels traffic, ensuring the origin only ever sees a tiny fraction of true global request volume |
| Origin protection | Dedicated origin shield layer | Guarantees at most one origin request per uncached item globally, regardless of how many edge/regional misses occur simultaneously |
| Request handling on miss | Coalescing (in-flight fetch deduplication) at every tier | Prevents thundering herd effects at each layer independently, not just at the origin |
| Invalidation | TTL (default) + explicit purge (escape hatch) | Balances operational simplicity for routine freshness against the need for near-immediate updates in urgent cases |
| Cache key design | Vary only on dimensions that genuinely affect response content | Avoids both correctness bugs (too narrow) and cache fragmentation (too broad) |
| Dynamic content | Bypass shared caching tiers, or use edge compute | Fully personalized content isn't a good fit for shared caching regardless of tier — forcing it through the cache hierarchy would provide no benefit and risk serving wrong content to the wrong user |

---

## 12. Bottlenecks & Scaling Considerations

- **Origin shield as a potential bottleneck** — while it dramatically reduces origin load, the shield itself must handle the aggregate miss traffic from ALL regional caches globally; needs to be horizontally scalable and highly available, since its failure would remove the origin's primary protection layer.
- **Purge propagation latency for very large fan-outs** — invalidating content across hundreds of globally distributed edge locations takes real time (seconds); for extremely time-sensitive invalidation needs (e.g., legal takedown requirements), this propagation delay must be accounted for in SLA commitments.
- **Cache hit ratio degradation from poor cache key design** — an overly granular cache key (accidentally varying on something like a session ID or timestamp) can silently tank hit ratio, effectively defeating the entire CDN's purpose for that content — this requires ongoing monitoring of hit ratios per content type/path pattern to catch regressions.
- **Regional tier sizing** — too few regional caches concentrates miss traffic (and thus load) onto fewer aggregation points; too many diminishes the traffic-consolidation benefit this tier is meant to provide — sizing depends on the specific geographic distribution of the platform's actual user base.
- **Cold cache scenarios (new region/PoP launch)** — a newly deployed edge location starts with an empty cache and will generate a temporary burst of cache misses cascading through the tiers; may warrant pre-warming for known-popular content ahead of a new PoP going live (same principle as the dedicated Cache Warming design).
- **Multi-CDN/failover strategies** — many large platforms use multiple CDN providers simultaneously for redundancy and negotiating leverage; this adds complexity around consistent cache key/invalidation behavior across genuinely different underlying CDN implementations, which is a substantial additional design consideration beyond a single-CDN architecture.
